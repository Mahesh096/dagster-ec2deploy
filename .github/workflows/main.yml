name: Deploy to EC2

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Clone Git Repository
      run: |
          cd dagster-ec2deploy
          sudo docker-compose up
          

